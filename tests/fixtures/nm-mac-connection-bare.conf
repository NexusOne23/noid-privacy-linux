# Fixture: NetworkManager conf.d drop-in using a BARE [connection] section
# (global connection-default form). Must keep parsing after the F-385 fix —
# regression guard so widening the sed range doesn't lose the bare case.
[connection]
ethernet.cloned-mac-address=random
